# Memory: skill-editor

Append-only. Newest entry on top. 2–3 sentences per entry.

---

2026-06-30: Skill retrofitted with eval.md and memory.md (predates skill-builder). Also fixed
the em dash rule itself: fixes were slipping through as comma-bracketed asides or parentheticals,
which is the same interruption with different punctuation, not an actual rewrite. The rule now
names that disguised pattern explicitly so it gets caught instead of waved through.
